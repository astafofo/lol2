from sklearn.svm import OneClassSVM
import numpy as np

X = np.array([[1,2],[2,3],[1,1],[10,10]])
clf = OneClassSVM(nu=0.1, kernel='rbf').fit(X[:3])
y_pred = clf.predict(X)
print("Task8: Insider Detection", y_pred)  # -1=anomaly
